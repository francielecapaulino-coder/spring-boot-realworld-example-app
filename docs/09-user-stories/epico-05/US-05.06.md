# US-05.06 — Validar migração completa e fechar KR1.4

## Identificação

| Campo | Valor |
|---|---|
| **ID** | US-05.06 |
| **Épico** | EPIC-05 — Migração MyBatis → Spring Data JPA + Hibernate |
| **Iniciativa** | INI-05 |
| **Fase** | Fase 4 |
| **Tipo** | `test` |
| **Prioridade** | 🔴 Crítica — gate formal do EPIC-05; libera INI-12 (soft delete) e INI-13 (reading time) |
| **Repositório** | `https://github.com/francielecapaulino-coder/spring-boot-realworld-example-app` |
| **Branch de trabalho** | `test/us-05.06-jpa-migration-validation` |
| **Depende de** | US-05.05 (MyBatis removido) |
| **Bloqueia** | INI-12 (EPIC-12 soft delete), INI-13 (EPIC-13 reading time), US-06.05 (elegibilidade de `Tag`/`FollowRelation` para record) |

> Pré-requisito: ler `docs/AGENTS.md` (REGRA ZERO + 6 ADRs ATIVOS) e `docs/00-original-mandate.md` (item J1) antes de implementar.

---

## Premissas da gestão atendidas por esta história

| Item do mandato | Como é atendida |
|---|---|
| J1 (`docs/00-original-mandate.md`) — "Migrate MyBatis to Spring Data/JPA/Hibernate" | Valida formalmente que a migração está 100% concluída: 0 referências MyBatis, testes verdes, KR1.4 atingido |
| G2 / G11 / G12 | Issue antes; commits `test`; documentação no Coda |

---

## História de usuário

**Como** tech lead do projeto,  
**quero** validar formalmente que a migração MyBatis → JPA está completa,  
**para que** o KR1.4 seja marcado como atingido e as iniciativas dependentes (EPIC-12, EPIC-13) possam ser iniciadas.

---

## Contexto

Após US-05.01 a US-05.05, a migração deve estar completa. Esta história executa a bateria de verificações definida no `INI-05` (Definition of Done) e documenta o resultado em `docs/process/tests-epico-05/`.

**KR1.4:** "0 mappers MyBatis remanescentes" — critério de saída do OKR 1 para EPIC-05.

**Impacto da conclusão deste épico:**
- EPIC-12 (soft delete) pode ser iniciado — depende de `@Where` do JPA
- EPIC-13 (reading time) pode ser iniciado — depende de JPA para persistir o campo calculado
- US-06.05 pode ser finalizado — `Tag` e `FollowRelation` agora são `@Entity` (inelegíveis para record)

---

## Escopo

* Executar checklist completo do INI-05 Definition of Done
* Executar `./gradlew test` e confirmar 100% verde
* Executar `./gradlew pitest` e confirmar score ≥ 95%
* Verificar via `grep` que 0 referências MyBatis existem no codebase
* Testar manualmente (ou via testes de contrato EPIC-08) os endpoints críticos:
  - `POST /users` (register)
  - `POST /users/login`
  - `GET /articles?tag=x&author=y&favorited=z`
  - `GET /articles/feed`
  - `GET /tags`
  - `POST /articles`
  - `PUT /articles/:slug`
  - `DELETE /articles/:slug`
  - `POST /articles/:slug/comments`
  - `DELETE /articles/:slug/comments/:id`
  - `POST /profiles/:username/follow` e `DELETE /profiles/:username/follow`
* Documentar resultado em `docs/process/tests-epico-05/migration-evidence.md`
* Marcar KR1.4 como ✅ em `05-backlog.md`
* Atualizar status de INI-12 e INI-13 em `03-initiatives.md` para "Desbloqueado"

## Fora de escopo

* Implementação de soft delete — EPIC-12
* Implementação de reading time — EPIC-13
* Otimizações de performance JPA (N+1, lazy/eager loading) — podem ser feitas como melhoria futura

## Critérios de aceite

* [ ] `grep -r "mybatis\|MyBatis\|org\.apache\.ibatis" src/` → 0 resultados
* [ ] `find src -name "*Mapper.java" -o -name "*Mapper.xml"` → 0 resultados
* [ ] `./gradlew test` → 100% verde
* [ ] `./gradlew pitest` → score ≥ 95%
* [ ] Todos os endpoints listados no escopo retornam resposta correta
* [ ] `docs/process/tests-epico-05/migration-evidence.md` criado com evidência
* [ ] KR1.4 marcado como ✅ em `05-backlog.md`

## Dependências

* US-05.05 concluído (MyBatis removido)

## Resultado esperado

EPIC-05 formalmente concluído. KR1.4 atingido. INI-12 e INI-13 desbloqueados.

## Observações para o agente

* Leia `docs/AGENTS.md` e `docs/03-initiatives.md` (INI-05 Definition of Done) antes de executar.
* Se qualquer verificação falhar, **não feche esta história** — voltar para a US correspondente e corrigir.
* Criar `docs/process/tests-epico-05/` se o diretório não existir.
* Após merge, atualizar `05-backlog.md` (KR1.4 = ✅, EPIC-05 = DONE) e fechar issue.
