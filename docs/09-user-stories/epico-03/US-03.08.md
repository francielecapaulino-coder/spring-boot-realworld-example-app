# US-03.08 — Atualizar `CONTRIBUTING.md` com guia de onboarding completo

## Identificação

| Campo | Valor |
|---|---|
| **ID** | US-03.08 |
| **Épico** | EPIC-03 — Containerização e ambiente local reproduzível |
| **Iniciativa** | INI-03 (`03-initiatives.md` v2.0) |
| **Fase** | Fase 1 (`04-roadmap.md` v5.0 — Marco M1) |
| **Tipo** | `docs` |
| **Prioridade** | 🔶 Alta — o CONTRIBUTING.md é o ponto de entrada para qualquer novo desenvolvedor |
| **Repositório** | `https://github.com/francielecapaulino-coder/spring-boot-realworld-example-app` |
| **Branch de trabalho** | `docs/us-03.08-contributing-onboarding` |
| **Depende de** | US-03.04 (Docker Compose funcional), US-03.05 (Grafana pré-configurado), US-03.06 (script de validação) |
| **Bloqueia** | US-03.09 (onboarding medido com desenvolvedor real usa este guia como referência) |

---

## Premissas da gestão atendidas por esta história

| Premissa | Como é atendida |
|---|---|
| *"Create issues before working, reference changes to repo to issues"* | Issue criada antes de qualquer mudança; commit referencia a issue com `closes #XX` |
| *"Use Conventional Commits for all commits"* | Tipo `docs` — documentação de processo |
| *"Code and commits with Coda"* | Prompts e etapas documentados no Coda antes de iniciar |

---

## História de usuário

**Como** desenvolvedor que acabou de clonar o repositório pela primeira vez,
**quero** que o `CONTRIBUTING.md` tenha um guia de onboarding completo e verificável que descreva como subir o ambiente local com Docker Compose —
**para que** eu consiga ter o sistema rodando em menos de 15 minutos sem precisar perguntar a ninguém.

---

## Contexto

O `CONTRIBUTING.md` atual (pós-US-02.06) descreve o fluxo de trabalho (Conventional Commits, bleeding branch, issues) e a configuração de variáveis JWT. Esta história adiciona a seção de onboarding completo para o ambiente Docker Compose, tornando o documento o único ponto de referência para um novo desenvolvedor.

### Critério do épico

**Onboarding em ≤ 15 minutos:** medido com desenvolvedor real (US-03.09), resultado documentado. O `CONTRIBUTING.md` é o roteiro desse onboarding.

### Pré-requisitos documentados

| Pré-requisito | Versão mínima | Verificação |
|---|---|---|
| Docker Desktop / Docker Engine | 24.x+ | `docker --version` |
| Docker Compose | v2.x+ | `docker compose version` |
| Python 3 | 3.10+ | `python3 --version` |
| Git | 2.x+ | `git --version` |

---

## O que esta história faz — e o que NÃO faz

### ✅ O que esta história faz

- Adiciona seção `## Onboarding — ambiente local com Docker Compose` ao `CONTRIBUTING.md`
- Documenta pré-requisitos com comandos de verificação
- Documenta o fluxo passo a passo: clone → `.env` → `docker compose up` → verificação → `docker compose down`
- Documenta URLs dos serviços (app, Grafana, Prometheus)
- Documenta como executar o script de validação `validate_startup.py`
- Documenta troubleshooting para os erros mais comuns

### ❌ O que esta história NÃO faz

- Não mede o tempo de onboarding — isso é US-03.09
- Não documenta ambiente de staging ou produção — fora do escopo
- Não atualiza o README com badges ou links — pode ser feito separadamente

---

## Critérios de aceite

| CA | Tipo | Descrição | Verificação |
|---|---|---|---|
| CA-01 | Estrutural | Seção `## Onboarding — ambiente local com Docker Compose` presente | `grep "Onboarding" CONTRIBUTING.md` |
| CA-02 | Conteúdo | Pré-requisitos listados com comandos de verificação | presença de `docker --version`, `docker compose version` |
| CA-03 | Conteúdo | Passo a passo: clone → `.env` → `docker compose up` → verificação | sequência completa documentada |
| CA-04 | Conteúdo | URLs dos serviços documentadas (app:8080, Grafana:3000, Prometheus:9090) | `grep "localhost:8080\|localhost:3000" CONTRIBUTING.md` |
| CA-05 | Conteúdo | Instrução para executar `validate_startup.py` | `grep "validate_startup" CONTRIBUTING.md` |
| CA-06 | Conteúdo | Seção de troubleshooting com erros comuns | presença de seção `### Troubleshooting` |
| CA-07 | Conteúdo | Comando `docker compose down` documentado para encerrar o ambiente | `grep "docker compose down" CONTRIBUTING.md` |

---

## Estrutura da seção a adicionar

```markdown
## Onboarding — ambiente local com Docker Compose

> Tempo estimado: < 15 minutos com conexão de internet estável.

### Pré-requisitos

| Ferramenta | Versão mínima | Verificação |
|---|---|---|
| Docker Desktop / Engine | 24.x+ | `docker --version` |
| Docker Compose | v2.x+ | `docker compose version` |
| Python 3 | 3.10+ | `python3 --version` |
| Git | 2.x+ | `git --version` |

### Passo a passo

1. Clone o repositório:
   git clone https://github.com/francielecapaulino-coder/spring-boot-realworld-example-app.git
   cd spring-boot-realworld-example-app

2. Crie o arquivo `.env` a partir do exemplo:
   cp .env.example .env
   # Edite .env e preencha JWT_SECRET e POSTGRES_PASSWORD
   export JWT_SECRET=$(openssl rand -base64 64)
   export POSTGRES_PASSWORD=$(openssl rand -base64 32)

3. Suba a stack completa:
   docker compose up -d

4. Aguarde os serviços ficarem saudáveis (~60s) e verifique:
   curl http://localhost:8080/actuator/health   # {"status":"UP"}
   curl http://localhost:8080/tags              # {"tags":[...]}

5. Acesse os serviços:
   - Aplicação:  http://localhost:8080
   - Grafana:    http://localhost:3000  (admin/admin)
   - Prometheus: http://localhost:9090

6. (Opcional) Valide o startup via script:
   pip install requests
   python3 scripts/validate_startup.py
   # Esperado: exit code 0 — ✓ Startup validated

7. Para encerrar o ambiente:
   docker compose down

### Troubleshooting

| Problema | Causa provável | Solução |
|---|---|---|
| `app` não sobe | `JWT_SECRET` ausente no `.env` | Gerar com `openssl rand -base64 64` |
| `postgres` unhealthy | Senha não configurada | Verificar `POSTGRES_PASSWORD` no `.env` |
| Porta 8080 ocupada | Outro processo na porta | `lsof -i :8080` e encerrar processo |
| Grafana sem fontes de dados | Volume de provisioning não montado | Verificar `docker-compose.yml` — seção grafana volumes |
```

---

## Verificações obrigatórias

```bash
# V1 — Seção de onboarding presente
grep "Onboarding" CONTRIBUTING.md
# Esperado: ## Onboarding — ambiente local com Docker Compose

# V2 — URLs dos serviços documentadas
grep "localhost:8080\|localhost:3000\|localhost:9090" CONTRIBUTING.md
# Esperado: 3+ ocorrências

# V3 — Script de validação referenciado
grep "validate_startup" CONTRIBUTING.md
# Esperado: ao menos 1 ocorrência

# V4 — Troubleshooting presente
grep "Troubleshooting" CONTRIBUTING.md
# Esperado: ### Troubleshooting

# V5 — Suíte Java não afetada (apenas docs alterados)
JWT_SECRET=test-secret-for-ci-at-least-32-characters-long \
  ./gradlew test --rerun-tasks --console=plain
# Esperado: BUILD SUCCESSFUL
```

---

## Definição de Done (DoD)

- [ ] Seção `## Onboarding — ambiente local com Docker Compose` adicionada ao `CONTRIBUTING.md`
- [ ] Pré-requisitos, passo a passo e troubleshooting documentados
- [ ] URLs dos 3 serviços (app, Grafana, Prometheus) documentadas
- [ ] `validate_startup.py` referenciado no guia
- [ ] Suíte Java → 73+ testes, 0 falhas (apenas docs alterados)
- [ ] Issue criada antes de iniciar (referenciada com `closes #XX`)
- [ ] Bleeding commit executado (`refs #XX`)
- [ ] PR aberta com título Conventional Commits
- [ ] Evidência de teste em `docs/process/tests-epico-03/test-evidence-us-03.08.md`

---

## Prompt para o Coda

```
== CONTEXTO ==
US-03.08 — Atualizar CONTRIBUTING.md com guia de onboarding completo
Repositório: https://github.com/francielecapaulino-coder/spring-boot-realworld-example-app
Branch: docs/us-03.08-contributing-onboarding
Critério: dev novo sobe o ambiente em < 15 minutos seguindo este guia

== ETAPA 1/2 — ADICIONAR SEÇÃO AO CONTRIBUTING.md ==
Arquivo: CONTRIBUTING.md
Adicionar seção "## Onboarding — ambiente local com Docker Compose"
Conforme estrutura documentada nesta US.

== ETAPA 2/2 — VERIFICAÇÕES E COMMIT ==
Executar V1 a V5 conforme seção de verificações.

Bleeding commit:
./scripts/bleeding-commit.sh \
  "docs(onboarding): step 1/1 - add Docker Compose onboarding guide to CONTRIBUTING.md" \
  "refs #XX"

Commit final:
git add CONTRIBUTING.md
git commit -m "docs(onboarding): add Docker Compose onboarding guide to CONTRIBUTING.md

- Add ## Onboarding section with prerequisites and step-by-step
- Document URLs: app (8080), Grafana (3000), Prometheus (9090)
- Reference validate_startup.py script
- Add Troubleshooting section for common issues
- Target: < 15 min onboarding for new developers (US-03.09)

closes #XX"
```

---

## Rastreabilidade

| Documento | Versão | Referência |
|---|---|---|
| `03-initiatives.md` | v2.0 | INI-03: "CONTRIBUTING.md com guia de setup completo e verificável" |
| `04-roadmap.md` | v5.0 | Fase 1 — Marco M1: "Dev novo subiu o ambiente em ≤ 15 minutos — tempo documentado" |
| `05-backlog.md` | v2.0 | EPIC-03, US-03.08: "Atualizar `CONTRIBUTING.md` com guia de onboarding completo" |
| `05-backlog.md` | v2.0 | Regra 1: "Onboarding em ≤ 15 minutos — medido com desenvolvedor real" |
| **Fork** | — | `https://github.com/francielecapaulino-coder/spring-boot-realworld-example-app` |
