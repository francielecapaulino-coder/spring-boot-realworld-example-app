# US-12.03 — Validar EPIC-12 completo e fechar KR de soft delete (J6)

## Identificação

| Campo | Valor |
|---|---|
| **ID** | US-12.03 |
| **Épico** | EPIC-12 — Soft delete de artigos e comentários |
| **Iniciativa** | INI-12 |
| **Fase** | Fase 5 |
| **Tipo** | `test` |
| **Prioridade** | 🔴 Crítica — gate formal do EPIC-12; libera fluxos 4 e 5 do Playwright (US-09.04) |
| **Repositório** | `https://github.com/francielecapaulino-coder/spring-boot-realworld-example-app` |
| **Branch de trabalho** | `test/us-12.03-soft-delete-validation` |
| **Depende de** | US-12.02 (soft delete de comentários concluído) |
| **Bloqueia** | US-09.04 (fluxos E2E 4 e 5 do Playwright) |

> Pré-requisito: ler `docs/AGENTS.md` (REGRA ZERO + 6 ADRs ATIVOS) e `docs/00-original-mandate.md` (item J6) antes de implementar.

---

## Premissas da gestão atendidas por esta história

| Item do mandato | Como é atendida |
|---|---|
| J6 (`docs/00-original-mandate.md`) — "Soft delete articles and comments" | Valida formalmente que artigos e comentários deletados não aparecem em listagens |
| G2 / G11 / G12 | Issue antes; commits `test`; documentação no Coda |

---

## História de usuário

**Como** tech lead,  
**quero** validar formalmente que o soft delete está funcionando para artigos e comentários,  
**para que** o KR J6 seja marcado como atingido e os fluxos Playwright 4 e 5 sejam desbloqueados.

---

## Contexto

Após US-12.01 e US-12.02, artigos e comentários deletados devem ter `is_deleted = true` no banco e não aparecer nas listagens. Esta história valida o comportamento end-to-end.

**Checklist de validação:**

| Comportamento | Verificação |
|---|---|
| `DELETE /articles/:slug` → artigo não aparece em `GET /articles` | Teste de integração |
| `DELETE /articles/:slug` → artigo não aparece em `GET /articles/:slug` (404) | Teste de integração |
| `DELETE /articles/:slug/comments/:id` → comentário não aparece em `GET /articles/:slug/comments` | Teste de integração |
| Registro físico permanece no banco com `is_deleted = true` | Consulta SQL direta via Testcontainers |

---

## Escopo

* Executar suite de testes de integração de EPIC-08 com soft delete ativo — confirmar 0 regressões
* Verificar diretamente no banco (via Testcontainers) que `is_deleted = true` após DELETE
* Documentar evidência em `docs/process/tests-epico-12/soft-delete-evidence.md`
* Marcar J6 como ✅ em `05-backlog.md`
* Atualizar status de US-09.04 em `05-backlog.md` para "Desbloqueado"

## Fora de escopo

* Restauração de registros soft-deleted
* Soft delete em outras entidades (follow, favorite, user)

## Critérios de aceite

* [ ] `./gradlew test` 100% verde após US-12.01 e US-12.02
* [ ] `DELETE /articles/:slug` → registro no banco com `is_deleted = true` — confirmado via SQL
* [ ] `GET /articles` após delete → artigo ausente da listagem
* [ ] `DELETE /articles/:slug/comments/:id` → comentário com `is_deleted = true` no banco
* [ ] `GET /articles/:slug/comments` após delete → comentário ausente
* [ ] `docs/process/tests-epico-12/soft-delete-evidence.md` criado
* [ ] J6 marcado como ✅ em `05-backlog.md`

## Dependências

* US-12.01 e US-12.02 concluídos

## Resultado esperado

EPIC-12 formalmente concluído. J6 atingido. US-09.04 desbloqueado.

## Observações para o agente

* Criar `docs/process/tests-epico-12/` se não existir.
* Após merge, atualizar `05-backlog.md` (EPIC-12 = DONE) e fechar issue.
