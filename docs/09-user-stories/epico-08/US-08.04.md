# US-08.04 — Testes de integração: Comments, Tags e segurança (Actuator)

## Identificação

| Campo | Valor |
|---|---|
| **ID** | US-08.04 |
| **Épico** | EPIC-08 — Testes de integração REST e GraphQL |
| **Iniciativa** | INI-08 |
| **Fase** | Fase 3 |
| **Tipo** | `test` |
| **Prioridade** | 🟡 Média — completa a cobertura dos 19 endpoints REST |
| **Repositório** | `https://github.com/francielecapaulino-coder/spring-boot-realworld-example-app` |
| **Branch de trabalho** | `test/us-08.04-integration-comments-tags` |
| **Depende de** | US-08.03 (Article endpoints cobertos) |
| **Bloqueia** | US-08.05 (GraphQL), US-08.06 (validação final) |

> Pré-requisito: ler `docs/AGENTS.md` (REGRA ZERO + 6 ADRs ATIVOS) e `docs/00-original-mandate.md` (itens G3 e G9) antes de implementar.

---

## Premissas da gestão atendidas por esta história

| Item do mandato | Como é atendida |
|---|---|
| G3 (`docs/00-original-mandate.md`) — "Integration tests for REST contract" | Cobre os endpoints de Comments e Tags completando os 19 contratos REST |
| G9 — "Testcontainers PostgreSQL" | Testes sobem PostgreSQL real via Testcontainers |
| G2 / G12 | Issue antes; commits `test`; documentação no Coda |

---

## História de usuário

**Como** desenvolvedor responsável pela qualidade,  
**quero** escrever testes de integração para os endpoints de comentários, tags e actuator,  
**para que** a cobertura dos 19 endpoints REST esteja completa.

---

## Contexto

Endpoints restantes:
- `CommentsApi.java` — `GET /articles/:slug/comments`, `POST /articles/:slug/comments`, `DELETE /articles/:slug/comments/:id`
- `TagsApi.java` — `GET /tags`
- `ActuatorSecurityTest.java` (já existe) — verificar se cobre `GET /actuator/health`

---

## Escopo

* Completar ou criar testes para:
  - `POST /articles/:slug/comments` — criar comentário (201), sem token (401)
  - `GET /articles/:slug/comments` — listar comentários (200), artigo inexistente (404)
  - `DELETE /articles/:slug/comments/:id` — deletar pelo autor (200), por outro (403), sem token (401)
  - `GET /tags` — lista de tags (200), resposta vazia válida (200)
* Verificar `ActuatorSecurityTest.java` — não duplicar

## Fora de escopo

* Testes GraphQL — US-08.05

## Critérios de aceite

* [ ] 3 endpoints de comments com ao menos 1 teste de sucesso e 1 de erro
* [ ] `GET /tags` com ao menos 1 teste de sucesso
* [ ] Total de endpoints REST cobertos: 19
* [ ] `./gradlew test` 100% verde com Testcontainers PostgreSQL

## Regras técnicas

* Reutilizar `TestHelper` para criar artigos e usuários de setup
* Autorização de DELETE de comentário: apenas o autor pode deletar (verificar `CommentsApiTest.java`)

## Dependências

* US-08.03 concluído

## Resultado esperado

19 endpoints REST com contrato validado por testes de integração.

## Observações para o agente

* Verificar `CommentsApiTest.java` e `ActuatorSecurityTest.java` antes de criar.
* Após merge, atualizar `05-backlog.md` e fechar issue.
