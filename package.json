{
  "name": "realworld-process-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js tools for development process — commitlint, Playwright E2E. Does not affect Java/Gradle build.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:docker": "docker compose up -d && playwright test"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "@playwright/test": "^1.61.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
